import React from 'react';
// import logo from './logo.svg';
import './App.css';
import MainWindow from './python/{{ projectName }}/gui/MainWindow/MainWindow';
import { MuiThemeProvider } from '@material-ui/core';
import theme from './theme';
import { BrowserRouter } from 'react-router-dom';
import {ChannelsSetup, GoogleSignInSetup} from './python/{{ projectName }}/gui/labbox';

function App() {
  return (
    <div className="App">
      <MuiThemeProvider theme={theme}>
        <BrowserRouter>
          <GoogleSignInSetup>
            <ChannelsSetup>
              <MainWindow />
            </ChannelsSetup>
          </GoogleSignInSetup>
        </BrowserRouter>
      </MuiThemeProvider>
    </div>
  )
}

export default App;
